import styled from 'styled-components'
import { themeS } from '../../consts/theme';

export const Navigation_Container = styled.nav`
    height: 95%;
    width: 100px;

    margin: 15px 0px 15px 15px;

    border-radius: 15px;

    background-color: ${themeS.mainTheme.colorMainBold};
`